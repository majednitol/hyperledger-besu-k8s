{
  "name": "besu-private-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Besu private permissioned network RIR registry and allowlist rules",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:local": "hardhat run deploy/deploy.js --network localhost",
    "deploy:besu": "hardhat run deploy/deploy.js --network besuPrivate"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
